node_modules
dist
.DS_Store
.vite

# Release signing — keep the keystore and its passwords out of git.
# BACK UP android/keystore/ + android/keystore.properties somewhere safe:
# a Play Store upload key must stay the same for the life of the app.
android/keystore/
android/keystore.properties

# Generated by `npx cap sync android` from dist/ — rebuild, never edit.
android/app/src/main/assets/public/

# Built APKs copied to the repo root for handing out
*.apk
